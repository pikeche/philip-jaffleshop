version: 2

sources:
    - name: stripe
      database: sandbox
      # schema: stripe    
      schema: PIKECHE      
      tables:
        - name: payment
          columns:
            - name: id
              description: Primary key for customers        
              tests:
                - unique
                - not_null        